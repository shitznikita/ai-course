# Sample Payments Project

Small fixture used by the Day 34 project-file assistant.

## Supported operation

Use `PaymentClient.charge(request)` to capture a payment.

Detailed API notes live in `docs/api.md`.
