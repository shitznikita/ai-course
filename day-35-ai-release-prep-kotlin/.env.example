# Copy to .env only for optional live `prepare`. Never commit the copied file.
LLM_API_KEY=replace-with-oauth-token

# Destination, auth scheme and model are fixed in code and are not configurable:
# https://api.eliza.yandex.net/openrouter/v1/chat/completions
# OAuth
# meta-llama/llama-3.3-70b-instruct
